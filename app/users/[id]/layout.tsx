"use client";

// This layout is intentionally empty - the root layout handles sidebar and navbar
export default function UserDetailLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}