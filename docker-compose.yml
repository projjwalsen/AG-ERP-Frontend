version: "3.9"

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ag-erp-frontend
    restart: unless-stopped
    env_file:
      - .env.local
    ports:
      - "3000:3000"
